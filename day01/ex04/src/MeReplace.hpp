#include <iostream>
#include <string>
#include <fstream>
#include <sstream>

class MeReplace
{

public:

    MeReplace(void);
    virtual ~MeReplace(void);

    void ft_replace(const std::string& file, const std::string& s1, const std::string& s2);

private:

};

